name: Node.js CI

on:
  push:
    branches: [ development ]
  pull_request:
    branches: [ development ]

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
 
    - run: npm i
    - run: npm run build 
